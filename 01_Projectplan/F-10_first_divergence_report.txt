F-10 — First Per-Trade Dart↔Rust Divergence Report
====================================================
Generated by: test/integration/dart_rust_first_divergence_test.dart (Sub-task F-10-A.1)
Engine build: bash tool/build_rust.sh release (fresh, exit 0)
Date:         2026-06-04

This is the PRE-FIX diagnostic snapshot. After the F-10-B fix the same
test must report `first_divergence = NONE`.

===== F-10 FIRST DIVERGENCE REPORT BEGIN =====
symbol            = BTCUSDT 1h
range UTC ms      = 1704067200000 .. 1719792000000 (exclusive)
fee_rate          = 0.0006
dart.trades       = 91
rust.trades       = 91
dart.total_pnl    = -1565.358744
rust.total_pnl    = -1514.059742
total_drift       = 51.299002
first_divergence at trade #0:
  entry_ts          = 1704801600000  (rust 1704801600000)
  exit_ts           = 1704888000000  (rust 1704888000000)
  direction         = LONG  (rust LONG)
  exit_reason       = StopLoss  (rust "StopLoss")
  dart.entry_price  = 46616.0
  rust.entry_price  = 46616.0
  dart.exit_price   = 44667.22
  rust.exit_price   = 44667.22
  dart.quantity     = 0.10256723837190564
  rust.quantity     = 0.09982329500943334
  qty_ratio (r/d)   = 0.973247370154173
  1/(1-fee_rate)    = 1.0006003602161297
  dart.gross_pnl    = -199.88098279440214
  rust.gross_pnl    = -194.53364084848337
  dart.implied_fees = 5.619322963210578
  rust.implied_fees = 5.467315079682606
  dart.pnl          = -205.50030575761272
  rust.pnl          = -200.00095592816598
  delta (r-d)       = 5.499349829446743
===== F-10 FIRST DIVERGENCE REPORT END =====

Analysis (subsystem localisation, PRE_TASK §3 Phase B)
------------------------------------------------------
- entry_price, exit_price, exit_ts, direction are IDENTICAL → no fill,
  slippage, or signal-logic drift. Trade count 91 == 91 confirms the
  signal logic is bit-equivalent (PRE_TASK §2.1).
- quantity DIFFERS: rust.qty / dart.qty = 0.97325. This is the union of
  TWO sizing drifts, both "Rust got the fix, Dart did not":

  (1) N-08 fee-aware sizing (DOMINANT, ~2.7%): all three Rust strategies
      emit `size_pct` via `position_size_pct_fee_aware`
      (rust/.../addins/bb_rsi.rs:183-201, called at bb_rsi.rs:497/521,
      ut_bot.rs:566/585, ichimoku.rs:711/732). The Dart engine still uses
      the legacy `risk_per_trade * close / sl_distance` (== Rust
      `position_size_pct`, the no-fee helper). The fee-aware denominator
      `sl_dist/entry + fee_rate*(1 + sl/entry)` shrinks the size.

  (2) N-13 entry-fee-no-shrink (RESIDUAL, factor 1/(1-fee)=1.0006): Rust
      `open_position` sets `quantity = alloc / entry_price`
      (mod.rs:496). Dart still sets `qty = (alloc - fee) / entry_price`
      (backtest_service.dart:761/778/1223/1240/1603/1620).

  0.97325 = (fee_aware/legacy) / (1 - fee_rate)  → both factors compose
  exactly to the observed ratio.

Fix plan (PRE_TASK §3 Phase C, Q2: atomic, B.1 then B.2):
  B.1 — port `position_size_pct_fee_aware` to Dart, replace all six legacy
        sizing call-sites. Re-run → expect residual N-13 drift only.
  B.2 — Dart `qty = alloc/entry`, close `balance += entry_notional + net_pnl`,
        shared `midTradeEquity` drop the `+ entryFee` inside `alloc`
        (the N-13 qty change invalidates that term). Re-run → divergence NONE.

Blast radius: the shared `midTradeEquity` helper and the identical root
cause make this a 3-strategy fix (bb_rsi + ut_bot + ichimoku). The
ut_bot and ichimoku parity tests are ALREADY red pre-fix with the same
pattern (ut_bot drift 13.30 USDT, ichimoku drift 2.77 USDT) and go green
with the same fix.
